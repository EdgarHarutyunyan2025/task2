name: Run Docker Compose Action

on:
  push:
    branches:
      - main 

jobs:
  docker-compose:
    runs-on: ubuntu-latest

    steps:
      - name: list dir 
        run: ls -la

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Docker Compose Action
        uses: ./
