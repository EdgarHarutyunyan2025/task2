name: Run Docker Action

on:
  push:
    branches:
      - main 

jobs:
  docker-compose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: list dir 
        run: |
          ls -la

      - name: Run Docker Action
        uses: ./
        with:
#          docker-compose-file: 'docker-compose.yml'
          input-file: 'text.txt'
          output-file: 'corect.txt'
